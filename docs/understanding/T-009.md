1. awake first, so all awake called before the first start runs. start run usually once before the first frame update or physics update. update called once in rendered frame, late update called after all update calls in the frame. fix update is called in interval of time.
2. fixed update is better because i can set how many times update called every second. then its flexible for multiple laws of physics, and also to make some variable changes on the project. its different with update which usually only runs once in rendered frame. update is based on framerate so its will be different with different device used but when use fixedupdate it doesnt care about the framerate, it cares only for the time interval.
3. deltatime is used on update whereas deltafixedtime is use for fixedupdate. deltatime is time since last rendered frame which is variable, but fixeddeltatime is fix steps because its used time interval.
4. it will chnage the update based on deltafixedtime that i put, so before it only update on every rendered frame now it based on the time that i set, which means run in fixed interval. which resulting consistent physics sim.
5. i think its basically just to initialize the particle before starting the simulation. awake is called and run before the update. so its safer to initialize the particle here, so when update runs, it guarantee that the particle already iniliased.