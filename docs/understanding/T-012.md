1. sph is smoothed particle hydrodynamics is a computation method to simulate fluid things with particle instead of fixed grid. SPH approximates continuous fluid behavior using moving particles that carry physical properties and interact with neighbors.
it solve problem that fixed grid have, which is free surface, grid based have to track interfacd explicitly, but sph is automatic because the particles are the surface. complex boundaries, grid based need mesh, but in sph no mesh needed. moving objects. grid based must have grid that constantly changing and adapting but in sph particle moves. adaptive resolution uniform, but sph particles concentrate where needed. mass conservation, grid based have to compute extra but sph guaranteed by the construction.
2. grid based that use navier stokes caannt be easily solved because problem with turbolance which remain one of the greatest unsolved problem in physics. they require to solving velocity and pressure simultaniously. and noview stokes are nonlinear pdes.
3. because each particle effection other neighboring particles, each particle have their own mass, velocity, acceleration. so the mass conservation is natural. eulerian have to pass the mass from cell to cell, whereas langrangian is the particle carry their one mass.
4. no mesh, no extra computation for mass conservation, no need to pass mass from cell to cell because particle have their own mass. 
5. smoothed is from ho sph approximates fluid properties. because the actual physics have continuous field, that change infinitely which our computer cannot hold that why we only have discrete particle. smoothed is for make that discrete to a field by approximates fields around the particle.