# T-022 — First Visual Validation (Dam Break)

This task is **validation, not implementation**. There is no new code to write. Instead you run the dam break, observe whether the solver behaves like a fluid, tune parameters, and record what you find.

Fill in the observation and parameter sections as you go. Be honest about what fails — the failures are what Phase 2 and 3 exist to fix.

---

## Setup

The defaults were changed to the classic dam break configuration:

| Field | Value | Why |
|---|---|---|
| `gravity` | `(0, -9.81)` | Gravity is back on — it is the driving force |
| `spawnRegionSize` | `(0.3, 0.8)` | A tall column: 30% of the width, 80% of the height |
| `spawnRegionCenter` | `(-1, 0)` | Flush to the left wall |
| `spawnCompression` | `1.0` | No artificial compression — gravity provides the disequilibrium |
| `numToSpawn` | `(10, 10)` = 100 particles | |
| `smoothingLengthInSpacing` | `2.2` | `h` is now derived, so neighbours stay ~15 for any particle count |
| `cflFactor` | `0.25` | Standard CFL safety factor |

Before running, work out and record the derived values (they're set at spawn):

- **particle spacing** = √(region area / particle count) = ______
- **smoothing length** `h` = `smoothingLengthInSpacing` × spacing = ______
- **expected neighbour count** ≈ π·h² / spacing² = ______
- **sound speed** `c_s` = √stiffness = ______
- **expected `dt`** ≈ `cflFactor` × h / c_s = ______

Turn on `debugLogs` for one frame to check the actual `steps=` and `dt=` against your estimate.

---

## Observation 1 — Collapse

Does the column fall under gravity?

- [ ] The column stays intact while falling (doesn't explode or fragment immediately)
- [ ] It collapses **downward**, not outward
- [ ] The top surface stays roughly flat while falling
- [ ] Nothing leaks through the floor

**What I saw:**


**Anything unexpected:**


---

## Observation 2 — Flow

Does the fluid spread along the floor?

- [ ] The fluid front advances to the right
- [ ] The leading edge is reasonably smooth, not spray
- [ ] The body of fluid stays connected rather than breaking into droplets
- [ ] There is an obvious low-pressure region behind the front

**What I saw:**


---

## Observation 3 — Splash

Does it hit the opposite wall?

- [ ] The front reaches the right wall
- [ ] There is a visible splash / run-up on the wall
- [ ] It does **not** penetrate the right wall
- [ ] Particles near the wall do not weld to it (this is what T-020 fixed)

**What I saw:**


---

## Observation 4 — Settle

Does it calm into a pool?

- [ ] Kinetic energy visibly decreases over time
- [ ] The surface levels out
- [ ] Motion stops (or nearly stops) rather than sloshing forever
- [ ] The final state is a pool roughly at rest density

**What I saw:**


---

## Parameter log

For each change, record what you changed and what happened. TASKS.md asks specifically about `stiffness`, `viscosity`, `spawnRegionSize`, and `dt`.

| Parameter | From → To | Effect observed |
|---|---|---|
| | | |
| | | |
| | | |
| | | |
| | | |

**Specific questions to answer:**

- **`stiffness`** — raise it to `8000`. What happens to the fluid's appearance? What happens to `dt` and the step count? This is the WCSPH trade-off, live.
- **`viscosity`** — set it to `0`. Does the fluid still settle? Set it to `2000`. Does it still flow?
- **`cflFactor`** — try `0.5`. Does it stay stable? If it breaks, at what point?
- **`spawnCompression`** — try `1.3`. What does the extra energy do to the run?
- **`useBoundaryParticles = false`** — what specifically gets worse?

---

## Failures worth recording

| Symptom | Likely cause | Which task fixes it |
|---|---|---|
| Spray / disconnected droplets everywhere | | |
| Slow — few FPS with only 100 particles | O(N²) neighbour search | T-023 |
| Fluid compresses too much / looks spongy | | |
| Particles clump or string at the surface | | |
| Blocks of particles moving in lockstep | | |

Fill in the causes where you can. Some of these are expected and are **the reason Phase 2 and 3 exist**.

---

## Verdict

**Does this look like a fluid?**

- [ ] Yes — collapse, flow, splash, settle all present
- [ ] Partly — which parts fail?
- [ ] No — describe what it actually looks like

**What I'd fix first, and why:**


---

## Checklist before leaving Phase 1

- [ ] Fluid collapses, flows, splashes, and settles
- [ ] Nothing escapes the container
- [ ] Simulation is stable at the default `cflFactor`
- [ ] Neighbour count is in a sane range (~15–30)
- [ ] I can predict roughly how a parameter change will affect the run
- [ ] I know which remaining problems belong to T-023 (spatial grid) vs T-024/025 (Burst/Jobs)
