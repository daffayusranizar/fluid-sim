1. assets is the main folder where i will work in unity. its basically the entiere project. scripts, scene, game objects, visual assets, and config all there. and theres also meta files which hold GUID that unity automatically assign and make. this meta files serialized every file in the assets folder, when i move some files it will auto generate the meta also, but not work when i move on finder
2. project is all the folder and files in the directory or project, hierarchy is for the list of active gameobject on the selected scene. scene is the visual editor where i can move around things. game is what the game actaually is, that are shown by the camera. instector is the propertry tab.
3. public field became inspector values, it kinda variable that we can adjust when running the game or in scene. its really usefull to adjust it then see the visualisation on the scene or game. unity serialized the public variables and expose it to the editor.
4. play mode behaviour, is where it actually run the game, it runs few funtion to keep the game running, then when you stop the play then it will back to the pre-starting state, then you can move to edit mode where you can move around objects and chnage script. unless i code executealways. and that executealways make the scene draws even if not in the play mode.
5. awake run once when the component initialize, it basically startup before first frame. update runs one every rendered frame. then inside it i can use deltatime