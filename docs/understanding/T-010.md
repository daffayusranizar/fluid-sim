1. vector really important in sph because in sph we need to know the distance for neighboring particle, direction for force like where will the particle move if theres some force applied to it and , the dot product for alignment checks.
2. in unity we can just use the Vector2.distance(a, b) which basically just computing the squareroot of ( delta x^2 + delta y^2). its used to compute neighbor distance from the particle.
3. dot prodcut measure how align vector are and useful for debug and checking the code. if same 1 if opposite -1 if perpendiculat 0.
4. to normalized direction use vector2.normalized which is (b - a).normalized. dir posb - posa then len equal to dir.magnitude then divide dir by len. we should guard againts the len, because it cannot be 0.
5. to be honest i not the one who wrote that code, so forget about the sph2d for now. we will rebuild it from scratch later. 