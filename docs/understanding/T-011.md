1. gizmo are editor only debug drawings, they appear on scene view, not the game view. Edit mode and Play mode.
2. ondrawgizmo is for all which means always and on every frame in the scene regardless of selected objects but ondrawgizmo selected is for only selected objects in the hierarchy.
3. how to color  Gizmos.color = Color.yellow;  , then draw line use .drawline, then .drawwiresphere(center, radius)
4. because gizmo only for visualization debug not to show text because it doesnt have text-drawing api.
5. gizmo not interactive handle yes, gizmo for only debigging but handles for tools and scene manipulations. gizmo only to draw, handles can make it draggable 